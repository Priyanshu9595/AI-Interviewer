{
  "name": "ai-interview-platform",
  "version": "1.0.0",
  "private": true,
  "description": "AI Interview Simulator Platform - run these scripts from the repository root",
  "scripts": {
    "dev": "concurrently -n api,web -c cyan,magenta \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "npm --prefix backend run dev",
    "dev:web": "npm --prefix frontend-v2 run dev",
    "build": "npm run build:api && npm run build:web",
    "build:api": "npm --prefix backend run build",
    "build:web": "npm --prefix frontend-v2 run build",
    "start:api": "npm --prefix backend run start",
    "start:web": "npm --prefix frontend-v2 run start",
    "typecheck": "npm --prefix backend run typecheck && npm --prefix frontend-v2 run typecheck",
    "setup": "npm --prefix backend install && npm --prefix frontend-v2 install",
    "db:push": "npm --prefix backend run db:push",
    "db:studio": "npm --prefix backend run db:studio",
    "seed": "npm --prefix backend run seed",
    "e2e": "npm --prefix backend exec tsx -- e2e.ts"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "workspaces": []
}